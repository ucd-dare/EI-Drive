test_scenario: record

single_cav_list:
  - name: cav1
    behavior:
      local_planner:
        debug_trajectory: false
        debug: false

# Parameters needed for ScenarioRunner
scenario_runner:
  routesConfig: '/home/ucdavis/project/EI-Drive/EIdrive/scenario_testing/test_scenario/lara_routes.xml'
  # Absolute path needed here
  routeId: '2'
  # Set 0 to record permanently
  max_record_ticks: 10